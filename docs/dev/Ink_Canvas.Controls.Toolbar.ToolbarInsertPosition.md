# <a id="Ink_Canvas_Controls_Toolbar_ToolbarInsertPosition"></a> Enum ToolbarInsertPosition

Namespace: [Ink\_Canvas.Controls.Toolbar](Ink\_Canvas.Controls.Toolbar.md)  
Assembly: InkCanvasForClass.dll  

```csharp
public enum ToolbarInsertPosition
```

## Fields

`AfterAnchor = 3` 

插入到由 AnchorName 指定的已有元素之后（同一锚点多项按 Order 依次排列）。



`Append = 1` 

追加到容器末尾。



`BeforeAnchor = 2` 

插入到由 AnchorName 指定的已有元素之前。



`Prepend = 0` 

从容器头部依次插入；Order 小的在前。



